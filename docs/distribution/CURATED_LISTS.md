# Curated List Targets

Submit only after early user feedback confirms the README is clear and the
safety model is understandable.

## Candidate Lists

- Awesome Rust: CLI/development tools section.
- Cargo subcommand collections, if actively maintained.
- Rust developer-tool roundups and newsletters.
- Agentic coding tool lists where Rust workflows are discussed.

## Suggested Blurb

```markdown
- [cargo-target-gc](https://github.com/MoonKibong/cargo-target-gc) - Cargo
  subcommand that scans and reclaims `target/` build artifacts by age/category,
  with dry-run-first cleanup and warm-cache preservation by default.
```

## Submission Notes

- Do not describe it as a safer `cargo clean` replacement.
- Do say it complements Cargo's own scoped clean options by adding artifact
  categorization and retention-based cleanup.
- Include the crates.io link where accepted:
  https://crates.io/crates/cargo-target-gc
